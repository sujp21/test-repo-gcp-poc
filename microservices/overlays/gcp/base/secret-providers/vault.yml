apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: vault-secret-provider-class
  labels:
    type: "microservice-secret"
spec:
  provider: gcp
  secretObjects:
    - secretName: vault-secrets
      type: Opaque
      data:
        - objectName: .env
          key: vault
  parameters:
    secrets: |
      - resourceName: "projects/1011757057153/secrets/vault/versions/latest"
        fileName: ".env"
