---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lns
spec:
  template:
    spec:
      containers:
        - name: lns
          volumeMounts:
            - name: lns-volumes
              mountPath: "/opt/code/.env"
              subPath: ".env"
              readOnly: true
          envFrom:
            - secretRef:
                name: lns-secrets
      volumes:
        - name: lns-volumes
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "lns-secret-provider-class"
