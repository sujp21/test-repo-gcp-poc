apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: dispatcher-secret-provider-class
  labels:
    type: "microservice-secret"
spec:
  provider: gcp
  secretObjects:
    - secretName: dispatcher-secrets
      type: Opaque
      data:
        - objectName: dispatcher
          key: dispatcher
  parameters:
    secrets: |
      - resourceName: "projects/712105902807/dispatcher/versions/latest"
        fileName: "dispatcher"
